<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema de Laboratorio - Registro</title>

    <!-- FONT AWESOME -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <!-- CSS PRINCIPAL -->
    <link rel="stylesheet" href="/Style1.css">

    <!-- ESTILOS DEL ENGRANE -->
    <style>
        .icon-group {
            position: relative;
        }

        #cogButton {
            background: none;
            border: none;
            font-size: 22px;
            cursor: pointer;
            color: var(--teschi-blue);
        }

        #cogButton:hover {
            transform: rotate(90deg);
            transition: 0.3s;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            right: 0;
            top: 40px;
            background: #ffffff;
            min-width: 230px;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.15);
            z-index: 9999;
            overflow: hidden;
        }

        .dropdown-content button,
        .dropdown-content a {
            width: 100%;
            padding: 13px 16px;
            border: none;
            background: none;
            text-align: left;
            cursor: pointer;
            font-size: 14px;
            display: flex;
            align-items: center;
            gap: 12px;
            color: #1f2937;
        }

        .dropdown-content i {
            width: 18px;
            text-align: center;
            font-size: 15px;
            color: #1f2937;
        }

        .dropdown-content button:hover,
        .dropdown-content a:hover {
            background: #f3f4f6;
        }

        .dropdown-content hr {
            margin: 0;
            border: none;
            border-top: 1px solid #e5e7eb;
        }

        .logout {
            color: #dc2626;
            font-weight: 500;
        }

        .logout i {
            color: #dc2626;
        }
    </style>
</head>

<body>

<div class="student-form-wrapper">

    <!-- HEADER -->
    <div class="dashboard-header">
        <h2>Registro de Acceso</h2>

        <!-- ⚙️ ENGRANE -->
        <div class="icon-group">
            <button id="cogButton" type="button" aria-label="Opciones">
                <i class="fa-solid fa-gear"></i>
            </button>

            <div class="dropdown-content" id="settingsMenu">

                <button type="button" onclick="reportProblem()">
                    <i class="fa-solid fa-triangle-exclamation"></i>
                    <span>Reportar Problema</span>
                </button>

                <button type="button" onclick="checkSystemStatus()">
                    <i class="fa-solid fa-circle-info"></i>
                    <span>Estado del Sistema</span>
                </button>

                <hr>

                <a href="/auth/logout" class="logout">
                    <i class="fa-solid fa-right-from-bracket"></i>
                    <span>Cerrar Sesión</span>
                </a>

            </div>
        </div>
    </div>

    <!-- FORMULARIO -->
    <form id="studentForm" method="POST" action="/alumno/registrar">

        <h3 class="section-title">1. Verificación</h3>

        <label for="studentCareer">Carrera</label>
        <select id="studentCareer" name="studentCareer" required></select>

        <label for="studentGroup">Grupo</label>
        <select id="studentGroup" name="studentGroup" required></select>

        <div id="statusMessage" class="status-disabled">
            Seleccione sus datos para verificar acceso.
        </div>

        <h3 class="section-title" id="machineTitle" style="display:none;">
            2. Datos de Bitácora
        </h3>

        <div id="registrationContent" class="registration-content">

            <div class="panel">
                <h4>Detalles del Usuario</h4>

                <label>Nombre Completo</label>
                <input type="text" readonly value="<%= user.nombre %>">

                <label>Matrícula / ID</label>
                <input type="text" readonly value="<%= user.email %>">

                <label for="machineNumber">Número de Equipo</label>
                <select id="machineNumber" name="machineNumber" required></select>

                <label for="observation">Observaciones</label>
                <textarea id="observation" name="observation"></textarea>
            </div>

            <div class="panel">
                <h4>Información de la Sesión</h4>
                <div class="info-box">Laboratorio: <strong id="infoLab">---</strong></div>
                <div class="info-box">Docente: <strong id="infoTeacher">---</strong></div>
                <div class="info-box">Grupo: <strong id="infoGroup">---</strong></div>
            </div>

        </div>

        <div class="btn-group">
            <button type="submit" id="registerButton" disabled>
                REGISTRAR ENTRADA
            </button>

            <button type="button" class="btn-update" onclick="checkAccess()">
                Actualizar
            </button>
        </div>

    </form>
</div>

<!-- JS -->
<script>
    const cogButton = document.getElementById("cogButton");
    const menu = document.getElementById("settingsMenu");

    cogButton.addEventListener("click", (e) => {
        e.stopPropagation();
        menu.style.display = menu.style.display === "block" ? "none" : "block";
    });

    document.addEventListener("click", () => {
        menu.style.display = "none";
    });

    function reportProblem() {
        window.location.href = "/reportar-problema";
    }

    function checkSystemStatus() {
        window.location.href = "/estado-sistema";
    }
</script>

</body>
</html>
